import 'package:flutter/cupertino.dart';

import 'ThemeData/ThemeData.dart';

class settingTap extends StatelessWidget {
  const settingTap({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: my_theme.redcolor,
    );
  }
}
